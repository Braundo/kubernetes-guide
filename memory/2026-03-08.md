# 2026-03-08

- Fixed generator duplicate-lead bug in `data/k8s_factory/generate.py` by making deck/body dedupe tolerant of truncated deck text ending in ellipsis.
- Added `_is_duplicate_deck_sentence` to detect prefix-equivalent first sentences, not just exact sentence matches.
- Cleaned duplicate opening sentence in published ecosystem article `docs/news/ecosystem/2026-03-08-deep-dive-simplifying-resource-orchestration-amazon-eks.md`.
- `py_compile` passes for pipeline modules.
- Could not run full `verify_generated_markdown()` in this environment due to missing local dependency: `feedparser`.
- Added hard style enforcement to ban em-dash/en-dash punctuation in generated content (`llm.py` rule + `editorial_quality.py` quality gate + `generate.py` sanitizer).
- Rewrote existing `docs/news/*.md` files to replace em-dash/en-dash punctuation with hyphens or comma-safe hyphen spacing.
