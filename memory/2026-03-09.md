# 2026-03-09

- Hardened pipeline duplicate prevention across planning, approval, and generation stages.
- Added semantic topic dedupe for short titles and GitHub repo identity matching (fixes duplicate k9s publication class of issue).
- Added generate-time same-day duplicate guard so pipeline skips duplicate topic instead of creating `-2` filenames.
- Added markdown sanitizer step to strip redundant H1/body title headings from generated drafts.
- Increased editorial depth requirements (words + sentence depth + ecosystem story depth checks) and updated prompts for stronger operator-focused analysis.
- Added custom topic request workflow with queueing and CLI manager in `data/k8s_factory/topic_requests.py`.
- Integrated custom topic requests into review flow in `run_pipeline.py`, including source matching and manual-topic approval visibility.
- Added internal link verification and same-day duplicate-topic verification in final markdown verification step.
- Rebuilt news indexes; removed stale broken tool-radar link to missing `k9s-2` page.
- Validation: `py_compile` passes for all touched pipeline modules; `.venv` verify check returns `verify_generated_markdown=True`.
