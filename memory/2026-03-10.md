# 2026-03-10

- Fixed discoverability bug where generated tool pages could publish without appearing on `docs/news/tool-radar/index.md` by forcing index sync in `run_pipeline.py` after generation and before verification/commit.
- Synced current Tool Radar index so `2026-03-09-headlamp.md` is now listed.
- Added new News subsection: `Playbooks` (`docs/news/playbooks/index.md`) for non-breaking operator thought pieces (HA, resiliency, architecture tradeoffs).
- Added pipeline/category support for `playbooks` across config, generation, quality checks, and prompts.
- Added manual-topic category preservation in generation so user-requested categories (including playbooks) are not auto-reclassified.
- Validation: `py_compile` passes and `.venv` verify returns `verify_generated_markdown=True`.
